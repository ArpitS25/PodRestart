pipeline {
    agent any

    stages {
        stage('SCM Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/ArpitS25/PodRestart.git'
            }
        }
    }
}
